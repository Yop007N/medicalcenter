## 2026-01-22 - Password Visibility Toggle Standard
**Learning:** Users heavily rely on password visibility toggles to avoid errors, especially with complex password requirements (regex). This is critical for accessibility and usability on mobile devices where typing precision is lower.
**Action:** Ensure all password fields in the application implement the standard "eye" toggle pattern using `ion-icon` and `ion-button` in `slot="end"`.
