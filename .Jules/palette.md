## 2024-03-24 - Add spinners to async buttons
**Learning:** Ionic provides built-in `<ion-spinner>` components. Adding `slot="start"` places it correctly alongside text inside `<ion-button>`s for async operations.
**Action:** Always include an `<ion-spinner>` inside `<ion-button>`s when they are in a loading state.
