## 2026-07-05 - Login Button Loading State
**Learning:** Adding a simple CSS spinner and `aria-busy` to the login button provides essential feedback during the potentially slow authentication process, especially since the component uses raw CSS rather than a utility framework.
**Action:** Always include loading indicators and `aria-busy` on async form submissions.
