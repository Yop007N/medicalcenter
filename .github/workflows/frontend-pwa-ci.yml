name: Frontend Paciente CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'frontend-paciente/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'frontend-paciente/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v3
      with:
        version: 10

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'pnpm'
        cache-dependency-path: frontend-paciente/pnpm-lock.yaml

    - name: Install dependencies
      working-directory: frontend-paciente
      run: pnpm install --frozen-lockfile

    - name: Run tests
      working-directory: frontend-paciente
      run: pnpm test -- --watch=false --code-coverage

    - name: Build
      working-directory: frontend-paciente
      run: pnpm run ionic:build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: frontend-paciente-build
        path: frontend-paciente/www/
