# E2E Frontend Profesional

## Ejecutar smoke

```bash
npm run e2e:chromium
```

Variables opcionales:

- `BASE_URL` (default `http://localhost`)
- `E2E_PROFESSIONAL_EMAIL` (default `doctor@medical.com`)
- `E2E_PROFESSIONAL_PASSWORD` (default `doctor123`)
