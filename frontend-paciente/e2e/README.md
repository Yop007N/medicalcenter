# E2E Frontend Paciente

## Ejecutar smoke

```bash
npm run e2e:chromium
```

Variables opcionales:

- `BASE_URL` (default `http://localhost:8100`)
- `E2E_PATIENT_EMAIL` (default `patient@medical.com`)
- `E2E_PATIENT_PASSWORD` (default `patient123`)
